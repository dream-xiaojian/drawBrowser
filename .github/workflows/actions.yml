on: 
  push:
    branches:
      - master

jobs:
  npm-build:
    runs-on: ubuntu-latest
    steps:
      - name: access rep
        uses: actions/checkout@v2

      - name: install dependencies
        run: npm install

      - name: build project
        run: npm run build